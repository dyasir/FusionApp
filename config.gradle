ext {

    /**
     * 需要改动的部分
     * 版本号和code值
     */
    versions = [
            versionCode      : 1,
            versionName      : "1.0.0"
    ]

    /**
     * 需要改动的部分
     * 包名
     */
    packages = [
            applicationId: "com.gloden.fishing.ground"
    ]

    /**
     * 需要改动的部分
     * APP名称
     */
    appName = [
            APP_NAME: "Gloden Fishing Ground"
    ]

    /**
     * 不需要改动的部分
     * 此处为打包签名信息，建议不要做更改
     */
    signingConfigs = [
            keyAlias     : "tikaapp",
            keyPassword  : "123456",
            storeFile    : "../tika.jks",
            storePassword: "123456"
    ]
}