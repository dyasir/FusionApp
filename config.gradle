ext {

    /**
     * 需要改动的部分
     * 版本号和code值
     */
    versions = [
            versionCode      : 1,
            versionName      : "1.0.0"
    ]

    /**
     * 需要改动的部分
     * 包名
     */
    packages = [
            applicationId: "com.yapersi.troce"
    ]

    /**
     * 需要改动的部分
     * APP名称
     */
    appName = [
            APP_NAME: "troic video"
    ]

    /**
     * 需要改动的部分
     * 默认域名
     * 是否生产环境
     */
    apiPath = [
            API_URL_TEST   : "http://172.247.143.109:85/",   //默认测试域名（若无法同步Firebase数据，则启用该域名）
            API_URL_PRODUCT: "https://app.109a.tv/",         //默认测试域名（若无法同步Firebase数据，则启用该域名）
            API_PRODUCT    : false                            //是否是生产环境
    ]

    /**
     * 不需要改动的部分
     * 此处为打包签名信息，建议不要做更改
     */
    signingConfigs = [
            keyAlias     : "tikaapp",
            keyPassword  : "123456",
            storeFile    : "../tika.jks",
            storePassword: "123456"
    ]
}